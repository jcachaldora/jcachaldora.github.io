# Personal site

One static HTML file. No build step, no dependencies, no analytics.
Fonts are loaded from Google Fonts; everything else is inline.

To publish: this repository is served by GitHub Pages from the root of the default branch.
